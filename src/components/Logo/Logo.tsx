import React, { FunctionComponent } from 'react';
import styles from './style.scss';

const Logo: FunctionComponent = () => {
  return <div className={styles.main}>WannaWatch</div>;
};

export default Logo;
